{
  "name": "docswap",
  "version": "1.0.0",
  "description": "Document conversion and sharing platform",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build completed successfully' && ls -la",
    "start": "vercel dev"
  },
  "dependencies": {
    "formidable": "^3.5.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}